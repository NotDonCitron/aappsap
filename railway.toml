[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn --bind 0.0.0.0:$PORT --workers 4 --timeout 120 'app:create_app(\"production\")'"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
